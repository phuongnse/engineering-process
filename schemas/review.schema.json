{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://engineering-process.invalid/schemas/review.schema.json",
  "title": "Independent review report",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "changeId", "reviewer", "checkpoint", "verdict", "summary", "findings"],
  "properties": {
    "schemaVersion": {"enum": [5, 6, 7]},
    "changeId": {"$ref": "#/$defs/id"},
    "reviewer": {"$ref": "#/$defs/actor"},
    "checkpoint": {"$ref": "#/$defs/checkpoint"},
    "verdict": {"enum": ["approved", "changes-requested"]},
    "summary": {"type": "string", "minLength": 1, "maxLength": 4000},
    "findings": {
      "type": "array",
      "maxItems": 128,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "severity", "priority", "criterionId", "origin", "summary"],
        "properties": {
          "id": {"$ref": "#/$defs/id"},
          "severity": {"enum": ["blocking", "non-blocking"]},
          "priority": {"enum": ["P0", "P1", "P2", "P3"]},
          "criterionId": {"$ref": "#/$defs/id"},
          "origin": {"enum": ["contract", "production-invariant", "remediation-regression", "critical-late"]},
          "summary": {"type": "string", "minLength": 1, "maxLength": 2000},
          "location": {"type": "string", "minLength": 1, "maxLength": 1000},
          "lateRationale": {"type": "string", "minLength": 1, "maxLength": 2000},
          "disposition": {"$ref": "#/$defs/disposition"}
        }
      }
    },
    "productionEngineering": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/invariantAssessment"}
    },
    "processImprovement": {"$ref": "#/$defs/processImprovement"}
  },
  "allOf": [
    {
      "if": {"properties": {"schemaVersion": {"enum": [6, 7]}}, "required": ["schemaVersion"]},
      "then": {
        "properties": {
          "findings": {
            "items": {
              "if": {"properties": {"severity": {"const": "non-blocking"}}, "required": ["severity"]},
              "then": {"required": ["disposition"]}
            }
          }
        }
      }
    },
    {
      "if": {"properties": {"schemaVersion": {"const": 7}}, "required": ["schemaVersion"]},
      "then": {"required": ["productionEngineering"]},
      "else": {"not": {"required": ["productionEngineering"]}}
    },
    {
      "if": {"properties": {"schemaVersion": {"const": 7}}, "required": ["schemaVersion"]},
      "then": {"required": ["processImprovement"]},
      "else": {"not": {"required": ["processImprovement"]}}
    },
    {
      "if": {"properties": {"schemaVersion": {"enum": [5, 6]}}, "required": ["schemaVersion"]},
      "then": {
        "properties": {
          "findings": {
            "items": {
              "properties": {
                "origin": {"enum": ["contract", "remediation-regression", "critical-late"]}
              }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "schemaVersion": {"const": 7},
          "verdict": {"const": "approved"}
        },
        "required": ["schemaVersion", "verdict"]
      },
      "then": {
        "properties": {
          "productionEngineering": {
            "items": {
              "properties": {"status": {"enum": ["satisfied", "not-applicable"]}}
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z0-9](?:[a-z0-9.-]*[a-z0-9])?$"
    },
    "actor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actorId", "contextId", "kind"],
      "properties": {
        "actorId": {"type": "string", "minLength": 1, "maxLength": 200},
        "contextId": {"type": "string", "minLength": 1, "maxLength": 200},
        "kind": {"enum": ["agent", "human"]}
      }
    },
    "disposition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "rationale"],
      "properties": {
        "status": {"enum": ["resolved", "accepted-risk", "tracked-follow-up"]},
        "rationale": {"type": "string", "minLength": 1, "maxLength": 2000, "pattern": "\\S"},
        "owner": {"type": "string", "minLength": 1, "maxLength": 200, "pattern": "\\S"},
        "recordUrl": {"type": "string", "minLength": 9, "maxLength": 1000, "pattern": "^https://\\S+$"}
      },
      "allOf": [
        {
          "if": {
            "properties": {"status": {"enum": ["accepted-risk", "tracked-follow-up"]}},
            "required": ["status"]
          },
          "then": {"required": ["owner", "recordUrl"]}
        }
      ]
    },
    "processImprovement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "rationale"],
      "properties": {
        "status": {"enum": ["none", "consumer-specific", "shared-process"]},
        "rationale": {"type": "string", "minLength": 1, "maxLength": 2000, "pattern": "\\S"},
        "recordUrl": {"type": "string", "minLength": 9, "maxLength": 1000, "pattern": "^https://\\S+$"}
      },
      "allOf": [
        {
          "if": {
            "properties": {"status": {"const": "shared-process"}},
            "required": ["status"]
          },
          "then": {"required": ["recordUrl"]}
        }
      ]
    },
    "invariantAssessment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "status", "rationale", "evidence"],
      "properties": {
        "id": {"$ref": "#/$defs/id"},
        "status": {"enum": ["satisfied", "not-applicable", "violated"]},
        "rationale": {"type": "string", "minLength": 1, "maxLength": 2000, "pattern": "\\S"},
        "evidence": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1, "maxLength": 1000, "pattern": "\\S"}
        },
        "findingId": {"$ref": "#/$defs/id"}
      },
      "allOf": [
        {
          "if": {
            "properties": {"status": {"const": "satisfied"}},
            "required": ["status"]
          },
          "then": {
            "properties": {"evidence": {"minItems": 1}},
            "not": {"required": ["findingId"]}
          }
        },
        {
          "if": {
            "properties": {"status": {"const": "not-applicable"}},
            "required": ["status"]
          },
          "then": {
            "properties": {"evidence": {"maxItems": 0}},
            "not": {"required": ["findingId"]}
          }
        },
        {
          "if": {
            "properties": {"status": {"const": "violated"}},
            "required": ["status"]
          },
          "then": {"required": ["findingId"]}
        }
      ]
    },
    "checkpoint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["head", "fingerprint", "fileCount", "byteCount"],
      "properties": {
        "head": {"type": ["string", "null"], "pattern": "^[0-9a-f]{40}(?:[0-9a-f]{24})?$"},
        "fingerprint": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
        "fileCount": {"type": "integer", "minimum": 0},
        "byteCount": {"type": "integer", "minimum": 0}
      }
    }
  }
}
